# Statistics with python cheetsheet

```python
import pandas as pd

# Mean
series.mean()

# Median
series.median()

# Mode
series.mode()

# Weighted mean
(numbers * means).sum() / weights.sum()
```
